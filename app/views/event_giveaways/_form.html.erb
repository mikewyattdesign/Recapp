<%= form_for(@event_giveaway) do |f| %>
  <% if @event_giveaway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_giveaway.errors.count, "error") %> prohibited this event_giveaway from being saved:</h2>

      <ul>
      <% @event_giveaway.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
      <%= f.select :event_id, options_from_collection_for_select(Event.all, :id, :name, @event_giveaway.event_id), {}, class: "" %>
  </div>
  <div class="form-group">
      <%= f.select :giveaway_id, options_from_collection_for_select(Giveaway.all, :id, :name, @event_giveaway.giveaway_id), {}, class: "" %>
  </div>
  <div class="form-group">
      <%= f.number_field :count_given_away %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
