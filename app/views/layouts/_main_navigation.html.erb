<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	<!-- Brand and toggle get grouped for better mobile display -->
	<div class="navbar-header">
		<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
			<span class="sr-only">Toggle navigation</span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
			<span class="icon-bar"></span>
		</button>

        <div class="navbar-header">
            <a class="navbar-brand" href="/">RecApp</a>
        </div>
		<!-- <a class="navbar-brand" href="#">Title</a> -->
	</div>

	<!-- Collect the nav links, forms, and other content for toggling -->
	<div class="collapse navbar-collapse navbar-ex1-collapse">
		<ul class="nav navbar-nav navbar-left">
    		<% if can? :read, Brand %>
    			<li class="<%= 'active' if request.path == '/brands' %>"><%= link_to "Brands", "/brands" %></li>
    		<% end %>
            <% if can? :read, Event %>
                <li class="<%= 'active' if request.path == '/events' %>"><%= link_to "Events", "/events" %></li>
            <% end %>
            <% if can? :read, Giveaway %>
                <li class="<%= 'active' if request.path == '/giveaways' %>"><%= link_to "Giveaways", "/giveaways" %></li>
    		    <% end %>
            <% if can? :read, Comment %>
                <li class="<%= 'active' if request.path == '/comments' %>"><%= link_to "Comments", "/comments" %></li>
            <% end %>

            <% if can? :create, Photo %>
            <li class="dropdown">
              <a id="resources-drop" href="#" class="dropdown-toggle" data-toggle="dropdown">Resources <b class="caret"></b></a>
              <ul class="dropdown-menu">
                <% if can? :read, Photo %>
                    <li class="<%= 'active' if request.path == '/photos' %>"><%= link_to "Photos", "/photos" %></li>
                <% end %>
                <% if can? :read, Video %>
                    <li class="<%= 'active' if request.path == '/videos' %>"><%= link_to "Videos", "/videos" %></li>
                <% end %>
                <% if can? :read, Contact %>
                    <li class="<%= 'active' if request.path == '/contacts' %>"><%= link_to "Contacts", "/contacts" %></li>
                <% end %>
                <% if can? :read, Document %>
                    <li class="<%= 'active' if request.path == '/documents' %>"><%= link_to "Documents", "/documents" %></li>
                <% end %>
              </ul>
            </li>
            <% end %>
		</ul>
		<ul class="nav navbar-nav navbar-right">
    		<% if user_signed_in? %>
    			<li><%= link_to destroy_user_session_path, method: :delete do %><span class="glyphicon glyphicon-log-out"></span> Logout<% end %></li>
    		<% else %>
    			<li><%= link_to new_user_session_path do %><span class="glyphicon glyphicon-log-in"></span> Login<% end %></li>
    		<% end %>
    		<li><%= link_to help_path do %><span class="glyphicon glyphicon-question-sign"></span><% end %></li>
		</ul>
	</div><!-- /.navbar-collapse -->

</nav>